#include <stdio.h>

void main(){
    //char First-Name; //Invalid because of hyphen
    char $Program;
    char _hello;
    char graPH02;
    //char wor.ld; //Invalid because of period
    //char int; //Invalid because a type identifier can't be used as a variable name
    //char time span; //Invalid because it contains a space
    //char 123enter; //Invalid because it starts with a numeric value
    char Lastname;
    char _99and;
}